import { WeekdaysNumberToStringPipe } from './weekdays-number-to-string.pipe';

describe('WeekdaysNumberToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekdaysNumberToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
